# HistoryBites backend env vars (Step 1 only — more will be added in later steps)

# Deployment environment label. Used for log context and future config switches.
ENVIRONMENT=development

# Python logging level (DEBUG, INFO, WARNING, ERROR).
LOG_LEVEL=INFO
